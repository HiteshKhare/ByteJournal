<h1>Posts</h1>
<% @posts.each do |post| %>
  <h2><%= post.title %></h2>
  <p><%= post.body %></p>
  <% if current_user&.author? && post.user == current_user %>
    <%= link_to 'Edit', edit_post_path(post) %>
  <% end %>
<% end %>
<%= link_to 'New Post', new_post_path if current_user&.author? %>
